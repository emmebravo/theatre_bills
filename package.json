{
  "name": "theatre_bills",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn packages/server start",
    "client": "yarn packages/client start",
    "server": "yarn packages/server dev",
    "build": "npm install && npm packages/client install && npm packages/client build"
  }
}
