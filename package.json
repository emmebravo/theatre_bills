{
  "name": "theatre_bills",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn packages/server start",
    "client": "yarn packages/client start",
    "server": "yarn packages/server dev",
    "build": "npm install && cd packages/client && npm install && npm build"
  }
}
