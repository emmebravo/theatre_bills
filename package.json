{
  "name": "theatre_bills",
  "engines": {
    "node": "18.x"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn packages/server start",
    "client": "yarn packages/client start",
    "server": "yarn packages/server dev",
    "build": "yarn ./packages/client install && yarn ./packages/client build && yarn ./packages/server install"
  }
}
